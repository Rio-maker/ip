package minerva.common;

/**
 * enum class to store all acceptable keywords, used in switch statements intially and printed when help prompted
 */
public enum Keyword {
    BYE, LIST, MARK, UNMARK, TODO, DEADLINE, EVENT, DELETE, TASK, UNKNOWN, HELP
}